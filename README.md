# Etihad Data Platform

This repository contains the Etihad Data Platform proof-of-concept project.

## Python environment

- Use Python 3.14
- Install dependencies with:

```bash
python -m pip install -r requirements.txt
```

## Compatibility note

`databricks-connect==16.1.7` is pinned for compatibility with Python 3.14.
